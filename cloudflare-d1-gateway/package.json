{
  "name": "zolexora-d1-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal authenticated proxy Worker exposing Cloudflare D1 to the FastAPI backend, following Cloudflare's documented pattern for reaching D1 from a non-Worker app.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0",
    "@cloudflare/workers-types": "^5.0.0"
  },
  "dependencies": {
    "hono": "^4.6.0"
  }
}
