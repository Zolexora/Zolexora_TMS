# MongoDB Event Plane
Used for high-volume telemetry, GPS, and dispatch events.
Namespaces: zolexora_tenant_001 through zolexora_tenant_010.
Initialized with `_platform_metadata` collection to ensure namespace creation in Atlas.
