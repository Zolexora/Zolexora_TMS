# Tenant Control Plane
Supabase is used exclusively as the identity and control plane. It holds the `organisations`, `memberships`, `tenant_database_registry`, and `platform_audit_logs`. It does NOT contain tenant business data.
